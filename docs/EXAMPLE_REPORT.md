# Example Report Notes

The smoke fixture should retain the local-only decision, preserve the release-candidate branch, mark failed or blocked items for verification, and redact the sample token and email.

Use the markdown output when handing context to a human reviewer. Use the JSON output when a downstream script needs counts or per-item classifications.
